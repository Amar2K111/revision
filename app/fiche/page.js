import FicheClient from "./FicheClient";

export const metadata = {
  title: "Fiche — Révision express",
  description: "Fiche de révision générée avec FlashRévis.",
};

export default function FichePage() {
  return <FicheClient />;
}
